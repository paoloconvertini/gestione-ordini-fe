import { HasPermDirective } from './has-perm.directive';

describe('HasPermDirective', () => {
  it('should create an instance', () => {
    const directive = new HasPermDirective();
    expect(directive).toBeTruthy();
  });
});
